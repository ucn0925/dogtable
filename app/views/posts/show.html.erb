<div class="container py-4 post-detail-page">

  <div class="mb-3">
    <%= link_to shop_path(@shop), class: "back-link" do %>
      <i class="fa-solid fa-chevron-left"></i>
      店舗ページに戻る
    <% end %>
  </div>

  <div class="post-detail-card">

    <!--ユーザー情報 -->
    <div class="post-header">

      <div class="post-user-wrap">

        <% if @post.user.profile_image.attached? %>
          <%= image_tag @post.user.profile_image, class: "avatar avatar-md" %>
        <% else %>
          <%= image_tag "default_user.png", class: "avatar avatar-md" %>
        <% end %>

        <div class="post-user-meta">
          <strong class="post-user">
            <%= link_to @post.user.name, user_path(@post.user) %>
          </strong>

          <% if @post.visited_on.present? %>
            <div class="post-visit-info">
              <span class="post-meta-pill">
                <i class="fa-solid fa-calendar-check"></i>
                <%= @post.visited_on.strftime("%Y/%m") %>
              </span>

              <% if @post.visit_scene.present? %>
                <span class="post-meta-pill">
                  <%= @post.visit_scene %>
                </span>
              <% end %>
              

              <% if @post.dog_size.present? %>
                <span class="post-meta-pill dog-info">
                  with
                  <%= case @post.dog_size
                    when "small" then "小型犬"
                    when "medium" then "中型犬"
                    when "large" then "大型犬"
                  end %>

                  <% if @post.user.dog&.dog_image&.attached? %>
                    <%= image_tag @post.user.dog.dog_image, class: "dog-icon" %>
                  <% else %>
                    <%= image_tag "default_dog.png", class: "dog-icon" %>
                  <% end %>
                </span>
              <% end %>

            </div>
          <% end %>

        </div>
      </div>

      <% if current_user == @post.user %>
        <div class="action-icons">
          <%= link_to edit_shop_post_path(@shop, @post), class: "action-icon" do %>
            <i class="fa-regular fa-pen-to-square"></i>
          <% end %>

          <%= link_to shop_post_path(@shop, @post),
                data: { turbo_method: :delete, turbo_confirm: "削除してよろしいですか？" },
                class: "action-icon delete-icon" do %>
            <i class="fa-regular fa-trash-can"></i>
          <% end %>
        </div>
      <% end %>

    </div>

    <!-- 評価 -->
    <div class="post-rating">

      <% [["総合評価", @post.rating_overall],
          ["料理", @post.rating_food],
          ["Dog Friendly", @post.rating_dog_friendliness]].each do |label, rating| %>
        
        <div class="post-rating-item">
          <div class="post-rating-label"><%= label %>：</div>
          <div>
            <% 5.times do |i| %>
              <span class="<%= i < rating.to_i ? 'dt-star' : 'dt-star-muted' %>">★</span>
            <% end %>
          </div>
        </div>

      <% end %>
    
    </div>

    <!-- コメント -->
    <div class="comment-section">

      <div class="dt-comment-box">
        <div class="dt-comment-text">
          <%= simple_format(h(@post.content)) %>
        </div>

        <% if @post.ordered_items.present? %>
          <div class="ordered-items">
            <div class="ordered-items-title">
              <i class="fa-solid fa-utensils"></i>
              注文したもの
            </div>

            <div class="ordered-items-list">
              <% @post.ordered_items.split(",").map(&:strip).reject(&:blank?).each do |item| %>
                <span class="ordered-item-badge">
                  <%= item %>
                </span>
              <% end %>
            </div>

          </div>
        <% end %>

        <!-- 投稿画像 -->
        <% if @post.images.attached? %>
          <div class="post-images">

            <% images = @post.images.first(2) %>

            <% images.each_with_index do |image, index| %>

              <%= link_to url_for(image),
                    class: "glightbox photo-wrapper",
                    data: { gallery: "post-#{@post.id}" } do %>
                    
                <%= image_tag image.variant(resize_to_limit: [250, 250]),
                      class: "post-image" %>
                
                <% if index == 1 && @post.images.count > 2 %>
                  <div class="photo-overlay">
                    + <%= @post.images.count - 2 %>
                  </div>
                <% end %>

              <% end %>
            <% end %>

            <% @post.images.drop(2).each do |image| %>
              <%= link_to "",
                    url_for(image),
                    class: "glightbox hidden-gallery",
                    data: { gallery: "post-#{@post.id}" } %>
  
            <% end %>

          </div>
          
        <% end %>

        <div class="dt-like-wrap">
          <div class="dt-like-left">
          
            <% if user_signed_in? %>

              <%= button_to shop_post_post_favorite_path(@shop, @post),
                    method: @post.favorited_by?(current_user) ? :delete : :post,
                    class: "dt-like-btn" do %>
              
                <% if @post.favorited_by?(current_user) %>
                  <i class="fa-solid fa-heart"></i>
                <% else %>
                  <i class="fa-regular fa-heart"></i>
                <% end %>
              
              <% end %>

            <% else %>

              <button class="dt-like-btn" disabled>
                <i class="fa-regular fa-heart"></i>
              </button>
            
            <% end %>

            <span class="dt-like-count">
              <%= @post.post_favorites.count %>
            </span>

            <% unless user_signed_in? %>
              <span class="login-message">
                お気に入り登録には
                <%= link_to "ログイン", new_user_session_path, class: "login-link" %>
                が必要です
              </span>
            <% end %>

          </div>

          <span class="dt-post-date">
            <%= @post.created_at.strftime("%Y/%m/%d %H:%M") %>
          </span>
        </div>

      </div>   
    </div>

  </div>
</div>