<h2><%= @user.name %>さんがフォロー中のユーザー</h2>

<% if @users.any? %>
  <ul>
    <% @users.each do |user| %>
        <li>
          <%= link_to user.name, user_path(user) %>
        </li>
    <% end %>
  </ul>
<% else %>
  <p>フォロー中のユーザーはいません。</p>
<% end %>