<h2><%= @user.name %>さんをフォローしているユーザー</h2>

<% if @users.any? %>
  <ul>
    <% @users.each do |user| %>
        <li>
          <%= link_to user.name, user_path(user) %>
        </li>
    <% end %>
  </ul>
<% else %>
  <p>フォロワーはいません。</p>
<% end %>