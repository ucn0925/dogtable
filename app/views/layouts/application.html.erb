<!DOCTYPE html>
<html>
  <head>
    <title>Dogtable</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

   <!-- GLightbox CSS -->
   <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/glightbox/dist/css/glightbox.min.css" />

   <!-- Glightbox JS -->
   <script src="https://cdn.jsdelivr.net/npm/glightbox/dist/js/glightbox.min.js"></script>
  </head>

  <body>
    <% if notice %>
      <div style= "background-color: #e6ffed; color: #056608; border: 1px solid #a3d9a5; padding: 10px; margin: 10px 0; border-radius: 8px;">
        <%= notice %>
      </div>
    <% end %>

    <% if alert %>
      <div style= "background-color: #ffe6e6; color: #8a0b0b; border: 1px solid #f3b6b6; padding: 10px; margin:10px 0; border-radius: 8px;">
        <%= alert %>
      </div>
    <% end %>

    <% if user_signed_in? %>
      <nav style="margin-bottom: 20px;">
      <%= link_to "トップページ", root_path %> |
      <%= link_to "マイページ", mypage_path %> |
      <%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete } %> 
    <% else %>
      <%= link_to "トップページ", root_path %> |
      <%= link_to "ログイン", new_user_session_path %> |
      <%= link_to "新規登録", new_user_registration_path %>
    <% end %>

    <%= yield %>

    <script>
    document.addEventListener("turbo:load", () => {
      if (window.GLightbox) {
        GLightbox({
          selector: '.glightbox',
          touchNavigation: true, 
          loop: true
        });
      }
    });
    </script>
      
    </body>
</html>
